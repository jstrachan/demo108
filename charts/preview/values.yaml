
expose:
  Annotations:
    helm.sh/hook: post-install,post-upgrade
    helm.sh/hook-delete-policy: hook-succeeded
  config:
    exposer: Ingress
    http: true
    tlsacme: false

cleanup:
  Args:
    - --cleanup
  Annotations:
    helm.sh/hook: pre-delete
    helm.sh/hook-delete-policy: hook-succeeded

preview:
  image:
    repository:
    tag:
    pullPolicy: IfNotPresent


postgresql:
  postgresUser: nestup
  postgresPassword: nestup
  postgresDatabase: nestup
  postgresConfig:
    max_prepared_transactions : 30
    max_connections: 30
keycloak:
  keycloak:
    persistence:
      dbVendor: postgres
      deployPostgres: false
      dbName: keycloak
      dbHost: postgresql
      dbPort: 5432
      dbUser: nestup
      dbPassword: nestup
    username: 'admin'
    password: 'test'    