apiVersion: v1
kind: Service
metadata:
  name: "keycloak"
  labels:
    app: "keycloak"
    chart: "keycloak-1.0.0"
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: keycloak
  selector:
    app: keycloak